services:
  heart-disease-app:
    build: .
    ports:
      - 8000:8000
    volumes:
      - ./mlflow.db:/app/mlflow.db
      - ./mlruns:/app/mlruns
      - ./config:/app/config
    environment:
      - MLFLOW_TRACKING_URI=sqlite:////app/mlflow.db
    restart: always
